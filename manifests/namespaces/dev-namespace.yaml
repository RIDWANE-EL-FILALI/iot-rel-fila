apiVersion: v1                    # Kubernetes API version
kind: Namespace                   # Resource type
metadata:                         # Resource metadata
  name: dev                       # Namespace name (DNS-compliant)
  labels:                         # Key-value pairs for organization
    environment: development      
    project: iot